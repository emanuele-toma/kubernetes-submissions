apiVersion: v1
kind: PersistentVolume
metadata:
  name: ping-log-pv
spec:
  storageClassName: ping-log-pv
  capacity:
    storage: 1Gi #
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  local:
    path: /tmp/kube
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - k3d-k3s-default-agent-0