apiVersion: v1
kind: Service
metadata:
  name: todo-app-svc
spec:
  type: ClusterIP
  selector:
    app: todo-app
  ports:
    - name: frontend
      protocol: TCP
      port: 2345
      targetPort: 3000

    - name: backend
      protocol: TCP
      port: 4000
      targetPort: 4000