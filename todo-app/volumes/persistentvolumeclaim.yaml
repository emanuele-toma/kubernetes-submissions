apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-app-claim
  namespace: project
spec:
  storageClassName: standard-rwo
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi